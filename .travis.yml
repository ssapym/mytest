language: php
php:
  - 5.3
  - 5.4
  - 5.5
  - 5.6

env:
  - DB=mysql

before_script:
  - sudo aptitude update > /dev/null
  - sudo aptitude upgrade > /dev/null
  - composer install
  - if [[ "$DB" == "mysql" ]]; then mysql -uroot < app/tests/yfrdb.sql; fi
script: phpunit
